// Placeholder module — replaced by the full-text search feature implementation.
export function registerSearchService(): void {
  // The real implementation registers its workspace extension on import.
}