// Placeholder module — replaced by the history/versioning feature implementation.
export function registerHistoryService(): void {
  // The real implementation registers its workspace extension on import.
}