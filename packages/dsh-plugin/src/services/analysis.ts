// Placeholder module — replaced by the structured book-analysis feature implementation.
export function registerAnalysisService(): void {
  // The real implementation registers its workspace extension on import.
}