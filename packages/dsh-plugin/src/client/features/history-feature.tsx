// Placeholder module — replaced by the history feature panel implementation.
export function registerHistoryFeature(): void {
  // The real implementation registers its workbench feature on import.
}